import {makeStyles} from '@material-ui/core';

export const UseStyleD = makeStyles((theme) => ({
   rootD : {
      width : '80%',
      height: 'auto',
      margin : 'auto',
      padding : '20px 0px',
   }
}));