import { makeStyles } from '@material-ui/core';

export const UseStyleFinal = makeStyles((theme) => ({
   cardMediaPokedex : {
      width : '130px',
      height : '130px',
      margin : 'auto'
   }
}))